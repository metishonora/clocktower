export function MathematicianAuditRowView({player,character,outcome,causes,timing}:{player:string;character:string;outcome:string;causes?:{type:string;label:string}[];timing?:string}){return <li className="snvMathematicianAuditRow"><div className="snvMathematicianAuditMain"><strong>{player}</strong><span className="snvMathematicianAuditCharacter">{character}</span><span className="snvMathematicianAuditOutcome">{outcome}</span></div>{causes?<div className="snvMathematicianAuditMeta"><span className="snvMathematicianAuditCauses" aria-label="원인">{causes.map(c=><em key={c.type} className={`snvMathematicianAuditCause ${c.type}`}>{c.label}</em>)}</span><time>{timing}</time></div>:null}</li>;}

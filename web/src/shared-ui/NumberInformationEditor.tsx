export function NumberInformationEditor({id,value,unit,error,hint,truthWarning=false,attempt=0,busy,onChange}:{id:string;value:string;unit:string;error?:string;hint?:string;truthWarning?:boolean;attempt?:number;busy:boolean;onChange:(value:string)=>void}){
 return <dl className="snvInformationValues snvSpaciousInformationEditor snvNumberConstraintEditor" aria-label="전달할 숫자 정보"><div><dt><label htmlFor={`delivered-${id}`}>전달할 정보</label></dt><dd><input id={`delivered-${id}`} aria-label="전달할 숫자" type="number" min="0" step="1" inputMode="numeric" value={value} disabled={busy} onChange={e=>onChange(e.target.value)}/><span>{unit}</span></dd></div>{error||hint?<p key={truthWarning?attempt:0} className={error?truthWarning?`snvInformationInputTruthWarning${attempt?' truthPulse':''}`:'snvInformationInputError':'snvInformationInputHint'} role={error?'alert':undefined}>{error??hint}</p>:null}</dl>;
}

{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --workspace-root build:wasm && vite --host 127.0.0.1",
    "build": "pnpm --workspace-root build:wasm && tsc -b && vite build",
    "test": "tsc -p tsconfig.test.json && node --test ../.codex-tmp/web-tests/setupDraft.test.js"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.3",
    "vite": "^6.0.7",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.1",
    "@types/react-dom": "^19.0.2"
  }
}

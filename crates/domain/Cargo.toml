[package]
name = "clocktower-domain"
version = "0.1.0"
edition = "2021"

[features]
default = []
custom-runtime-fixtures = []

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

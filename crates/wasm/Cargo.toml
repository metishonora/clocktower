[package]
name = "clocktower-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[features]
default = []
custom-runtime-fixtures = ["clocktower-domain/custom-runtime-fixtures"]

[dependencies]
clocktower-domain = { path = "../domain" }
wasm-bindgen = "=0.2.100"

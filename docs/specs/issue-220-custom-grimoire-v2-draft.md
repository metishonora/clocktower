> 보관된 승인 전 초안입니다. 사용자 승인 이후 기준 문서는 [Spec v3](issue-220-custom-grimoire.md)입니다. 아래 대기 표시는 당시 검토 이력입니다.

## Spec 초안 v2 — #220 ([205-2]) 기존 SnV 마도서 기반 커스텀 시나리오·첫날 밤 연결

> 상태: 사용자 전체 문안 승인 전. 기존 GitHub Spec을 대체하지 않는 검토 초안이다. 저장 충돌 정책 S1은 아래 권장안에 대한 사용자 답변을 기다리고 있다.

### 1. 기준과 문서 상태

2026-09-10 사용자 결정에 따라 [#205 원본 Spec](https://github.com/metishonora/clocktower/issues/205#issuecomment-5595997690)을 #220으로 복사·계승한다. #205 원본과 승인된 프로토타입은 변경하지 않는다. 이 초안은 아래 최종 승인 사항을 반영해 #220의 구현 기준을 갱신하려는 문서이며, 원본의 내부 시나리오 보관 요구와 미결정 항목을 그대로 재도입하지 않는다.

- 작성·파일 계약: [#197 Spec](https://github.com/metishonora/clocktower/issues/197#issuecomment-5594316985)과 [완료 기록](https://github.com/metishonora/clocktower/issues/197#issuecomment-5595515155)
- 시각·상호작용: [#202 최종 승인 결과의 #205 반영](https://github.com/metishonora/clocktower/issues/205#issuecomment-5559777329), 그리고 2026-09-09 #205 시나리오 편집 최종 승인
- 최종 승인 근거: 보존된 `web/src/prototypes/issue205/NOTES.md`의 Current user decisions, compact source panel, direct JSON import, User approval 항목. #220에는 `docs/prototypes/issue-220/issue-205-editor-decisions.md`로 원문을 보존한다.
- 순서·저장 기반: [#198 Intent](https://github.com/metishonora/clocktower/issues/198#issuecomment-5559905991)
- 첫날 밤 실행: #203 및 #206–#209의 최종 승인 계약과 완료 결과
- 분기 경위: [#205 보류 기록](https://github.com/metishonora/clocktower/issues/205#issuecomment-5601329450), [#220 독립 연결 결정](https://github.com/metishonora/clocktower/issues/205#issuecomment-5619624031)

#202·#197·#203은 완료된 선행 기반이다. #205 초기 본문의 외부 공식 custom-script JSON 변환과 별도 Setup 행동 목록 요구는 적용하지 않는다. #197 기반의 실제 파일·검증 기능을 재사용하고, 편집 화면과 상호작용은 그 뒤 승인된 #205 최종본으로 맞춘다.

#220은 승인된 편집 흐름 뒤에 기존 SnV 형태의 마도서를 연결한다. #218의 이야기형 UI 승인에 의존하지 않는다. #218은 같은 커스텀 도메인·세션 연결을 사용하는 별도 표현으로 발전하며, 이 분기로 게임 규칙·저장 계약을 복제하지 않는다.

#205 편집 화면 승인과, 사용자가 검토 후 Spec 단계로 진행하기로 한 #220 대표 연결 프로토타입을 UI 근거로 사용한다. 재확인 가능한 화면은 `/clocktower/issue-220-prototype.html`, 검토 기록은 `web/src/prototypes/issue220/NOTES.md`와 `docs/prototypes/issue-220/connection/`에 있다.

확인된 연결은 최종 검토 → 로딩 → 기존 SnV 형태의 직업·마도서·진행 탭, 빈 actual roster에서 역할·좌석 설정, 첫날 밤 정보 공개·가리기·다음, exact-match 이어 쓰기다. 공개 중 이야기꾼 UI와 검토 도구를 숨기고, 가린 뒤 진행한다. 숫자 정보의 크기를 포함한 기존 SnV 표현을 유지한다.

프로토타입의 고정 분포표·5인 배역·숫자값·순서 필터·재개 위치·메모리 상태는 설계나 규칙 계약으로 채택하지 않는다. 모든 TB/SnV 첫날 밤 행동이 프로토타입에서 검토·실행됐다고 취급하지 않는다. 현재 부분 Setup 연결과 대표 예시는 실제 파일 처리·첫날 밤 전체 실행·지속 저장의 구현 완료를 뜻하지 않는다.

### 2. 사용자 흐름과 UI

1. Production Custom Scenario 진입에는 `새롭게 작성한다`와 `JSON에서 불러온다`만 제공한다. `저장본을 연다`와 앱 내부 시나리오 목록은 제공하지 않는다.
2. 새 작성은 시나리오 선택 → 캐릭터 설정·이름 → 밤 행동 순서 → 최종 검토로 이어진다. 이번 범위에서 편집·저장·실행하는 밤은 첫날 밤뿐이다.
3. JSON은 목적 선택 없이 하나의 진입점으로 불러온다. 성공하면 곧바로 최종 검토로 이동한다. 파일명·진행 요약·중간 검토 버튼·공식 마도서 분기 대화상자를 추가하지 않는다. 실패하면 파일 선택 화면에서 오류를 표시하고, 취소하면 현재 내용을 유지한다.
4. 최종 검토의 `시나리오 저장`은 즉시 JSON 다운로드를 요청한다. `새 마도서 쓰기`는 추가 확인 대화상자 없이 기존 TB/SnV 계열 로딩 전환을 거쳐 마도서 게임 설정으로 이동한다. 별도 Setup 행동 목록·첫날 밤 미리보기·추가 시나리오 확인 화면은 두지 않는다.
5. 참가자·좌석·Actual/Shown Character와 게임별 Setup은 기존 SnV 형태의 마도서에서 설정한다. 시나리오 후보 풀을 실제 참가 배역으로 자동 간주하지 않는다.
6. 복원 가능한 불러온 게임과 현재 편집본이 정확히 일치할 때만 `마도서 이어 쓰기`를 제공한다. 시나리오 파일만 불러온 경우에는 게임 진행 정보를 만들거나 재개를 활성화하지 않는다.

시작 패널은 내용에 맞는 높이로 화면 아래에 배치한다. 불필요한 별 장식·업로드 아이콘·도움말과 `스크립트 선택으로` 버튼을 재도입하지 않는다. JSON 파일 선택은 최소 44px 높이의 간결한 버튼을 유지한다.

#197에서 연결한 검색·필터·선택·상세, 위/아래 순서 이동, 고정 dusk/dawn, 기본값 복원, 반응형·키보드·터치·reduced-motion 동작을 유지한다. 드래그 재배치와 지속적인 단계 표시기는 추가하지 않는다.

최종 검토는 #205 최종 승인 기준의 이름 편집, 진영별 구성 수와 전체 캐릭터 아이콘·이름, 승인된 주요 행동을 유지한다. 밤 순서 요약·구성 수정 버튼·출처·상단 배지·일반 알림 패널을 다시 추가하지 않는다. 모바일에서는 내용과 행동·이전 단계 복귀에 하나의 흐름으로 접근할 수 있어야 한다.

이름 → 캐릭터 설정 → 밤 행동 순서 순으로 첫 차단 오류 하나를 표시한다. 15인 권장 후보 수 13/4/4/4 부족은 비차단 경고이며, 실제 게임 시작 가능 여부는 별도의 Setup 유효성에 따른다. 저장·불러오기·실행 실패에는 원인에 맞는 간결한 복구 동선을 제공한다.

### 3. 시나리오 파일

| 작업 | 보장할 결과 |
| --- | --- |
| 시나리오 저장 | #197과 동일한 앱 전용 JSON을 즉시 다운로드. 내부 시나리오 저장본이나 게임을 생성·교체하지 않음 |
| 시나리오 JSON 불러오기 | 파일의 이름·캐릭터 배열·첫날 밤 순서를 검증 후 복원하고 최종 검토로 이동. 진행 기록이나 이어 쓰기 자격을 만들지 않음 |
| 게임 진행을 포함한 지원 JSON 불러오기 | 별도 목적 선택 없이 검증 후 최종 검토로 이동. 유효한 커스텀 게임과 exact identity 일치 시에만 이어 쓰기 제공 |

시나리오는 자동 저장하거나 앱 내부에 별도로 보관하지 않는다. 저장본 목록·복사·교체·복구와 새 마도서 생성 중 시나리오 보관 선택을 추가하지 않는다. 게임 진행의 자동 저장은 별도 책임이며 6절 계약을 유지한다.

시나리오 파일에는 내부 저장 식별자나 게임 진행 기록을 추가하지 않는다. 같은 시나리오 파일을 반복해서 불러와도 기존 게임을 찾아 연결하거나 교체하지 않는다.

모든 다운로드와 새 마도서 생성은 현재 내용의 유효성을 기준으로 한다. 검증 이후 내용이 바뀌면 이전 성공 결과로 변경된 내용을 저장하거나 새 마도서를 만들 수 없다. 파일 선택 취소·읽기 실패·검증 실패와 늦은 비동기 결과로 현재 편집 대상을 덮어쓰지 않는 #197 계약을 유지한다.

### 4. 새 마도서와 게임 설정

- 새 마도서는 최종 검토에서 선택한 유효한 시나리오의 일관된 definition snapshot을 사용한다. 이후 별도 편집본의 변경을 진행 중 게임에 적용하지 않는다.
- 마도서의 게임 설정에서 참가자·좌석·실제 배역·Shown Character·bluff 후보와 인원별 진영 분포 및 Setup 변경 효과를 기존 커스텀 도메인 계약으로 검증한다.
- 실제 배역, 표시 배역, bluff 등 각 후보의 허용 조건은 기존 규칙을 따른다. 시나리오 밖 캐릭터를 선택하거나 사용하게 허용하지 않는다.
- 시나리오 후보 수 부족 경고와 실제 배역 구성 오류를 구분한다. 시나리오가 유효해도 실제 Setup이 유효하지 않으면 게임 시작을 거부하고 설정을 수정할 수 있게 한다.
- Setup 확정에 성공하면 해당 시나리오와 실제 게임 설정으로 첫날 밤을 진행한다. 거부된 설정을 일부만 적용하거나 성공한 것처럼 다음 단계로 이동하지 않는다.
- 게임별 행동 순서 편집이나 별도 순서 저장을 추가하지 않는다.

최종 승인대로 기존 게임이 불러와져 있어도 `새 마도서 쓰기`에서 추가 확인 없이 게임 설정으로 이동한다. 설정 화면에 들어가는 것만으로 기존 게임을 삭제하거나 덮어쓰지 않는다. 새 게임의 저장과 기존 게임 보존은 기존 커스텀 세션 계약을 따르며, 설정·저장 실패를 성공으로 처리하지 않는다.

### 5. 이어 쓰기와 게임 파일

**커스텀 게임**

- 이어 쓰기는 유효하게 읽고 전체 진행 기록을 복원할 수 있는 커스텀 게임을 대상으로 한다. definition 일치만으로 손상되거나 재생할 수 없는 게임을 재개하지 않는다.
- 게임 파일에 담긴 원래 definition과 현재 편집 대상이 기존 exact identity 계약에서 정확히 일치해야 한다.
- 기존 계약의 비교 대상은 `id`, `name`, 순서를 포함한 `characterIds`, 각 행동의 식별과 순서를 포함한 `firstNightOrder`다. 표시 문자열 정규화나 집합 비교로 동등성을 넓히지 않는다. 따라서 이름만 바꾸거나 캐릭터 배열 순서만 바꿔도 일치하지 않는다.
- 실제 참가하지 않은 후보 캐릭터를 변경해도 이어 쓰기를 허용하지 않는다. 현재 사용된 배역·과거 이벤트의 참조 여부만으로 비교 범위를 좁히지 않는다.
- 변경된 편집본에서는 새 마도서만 허용하고, 원래 값으로 정확히 되돌리면 이어 쓰기 가능 상태를 복구한다. 편집과 비교 과정에서 원래 게임을 변경하지 않는다.
- 시나리오 JSON에는 게임 identity와 진행 기록이 없으므로, 내용이 비슷하다는 이유로 기존 게임을 찾아 이어 붙이지 않는다.

**공식 게임과 파일 경계**

JSON의 사용자 진입은 하나로 통합하되 시나리오·커스텀 GameFile·공식 파일의 타입 및 검증 책임은 섞지 않는다. #197 시나리오 전용 판독기의 수용 범위를 무조건 넓히지 않고, 지원하는 커스텀 GameFile은 해당 검증·복원 경계로 처리한다.

공식 TB·SnV 게임의 재개는 기존 공식 화면·파일 계약에 남긴다. #220 편집기에 공식 게임 재개/커스텀 변환 선택이나 별도 공식 분기 화면을 만들지 않는다. 공식 게임을 커스텀 실행으로 변환하거나 커스텀 로딩 실패 시 공식 실행으로 우회하지 않는다. 지원하지 않는 파일은 간결한 오류로 처리한다.

게임 파일 검증·복원 실패는 기존 정상 게임과 현재 편집 내용을 보존하고 재시도할 수 있게 한다.

### 6. 첫날 밤·저장·복원

- 첫날 밤은 #203 및 #206–#209에서 승인된 행동·정보 전달·후속 결과를 기존 실행 경계로 제공한다. 새 캐릭터 규칙이나 별도 웹 판정을 추가하지 않는다.
- 시나리오의 명시적 순서와 행동 시점의 상태에 따라 실제 적용되는 행동을 처리한다. 시나리오에만 있는 행동을 실행하거나 필요한 다중 소유자·획득 능력 행동을 누락하지 않는다.
- 실행·복원 중 누락된 순서를 작성용 기본안으로 보충하지 않는다.
- 첫날 밤은 승인된 규칙에 따라 Day 또는 게임 종료 상태에 도달한다. 이후 밤 실행 및 낮의 일반 진행·능력 실행을 이번 UI 연결 범위로 확대하지 않는다.
- 자동 저장·새로고침·Undo·게임 파일 내보내기와 불러오기 후 동일한 definition snapshot과 확정 기록으로 동일한 상태·진행 위치·확정 당시 전달 정보를 복원한다. Undo 단위와 규칙은 기존 세션 계약을 따른다.
- 저장 실패는 게임 명령 거부나 파일 손상과 구분하며, 디스크에 저장되지 않은 상태를 저장 완료로 표시하지 않는다. 메모리에서 성공한 진행과 마지막 저장 상태가 다를 수 있는 경우 그 상태를 오인하지 않게 하고 재시도할 수 있어야 한다.
- 손상된 저장본이나 replay 실패를 정상 빈 게임으로 대체하지 않는다. 기존 게임 저장·복구 계약을 재사용한다. 그 계약으로 정해지지 않은 파괴적 교체 정책이 실제로 필요할 때만 10절 기준에 따라 별도로 결정한다.

### 7. 시스템 책임과 제약

| 경계 | 책임 |
| --- | --- |
| 기존 시나리오 작성·파일 기능 | #197의 편집·검증 결과·파일 왕복·비동기 최신성 보호 유지 |
| 마도서 UI | 기존 SnV 화면 구조를 재사용한 게임 설정 입력, 현재 행동·전달 정보 표시, 확정·Undo·저장 상태와 오류 동선 |
| 커스텀 도메인·WASM | 시나리오·Setup·행동·결과 검증, 순서·캐릭터 규칙과 replay |
| 커스텀 세션·게임 파일 | definition snapshot과 확정 이벤트, 자동 저장·복원·Undo·파일 왕복 |
| 공식 화면·저장 | 기존 공식 실행·게임 파일 처리와 독립적인 저장 동작 유지 |

`definition.firstNightOrder`가 유일한 확정 순서이며 `createGame`·`setupConfirmed`에 별도 `firstNightOrderPlan`을 복제하지 않는다. Setup 행동 목록을 별도 UI나 저장 상태로 만들지 않는다.

커스텀 연결 계층은 편집 결과·Setup 입력·현재 세션·화면 상태 사이의 전환과 비동기 최신성을 조정한다. 이 계층이 외부 공용 표현 컴포넌트에 데이터와 콜백을 공급하고, 커스텀 도메인·세션 내부는 공용 UI나 공식 실행 코드에 의존하지 않는다. 기존 import 경계 검사를 완화하지 않는다. #218 역시 이 커스텀 연결 계약을 소비할 수 있으며, 지금 이야기형 전용 API나 범용 플러그인 구조를 추가하지 않는다.

첫날 밤 화면은 실제 replay의 `currentStep`, `availableActions`, 필수 준비 및 후속 공개 상태를 표시한다. UI가 캐릭터 이름만으로 행동을 추측하거나 `firstNightOrder`를 roster로 단순 필터링하여 런타임 순서를 대신하지 않는다. 여러 소유자·획득 능력·재준비는 런타임 occurrence와 출처를 유지해 서로 구별한다.

사용자가 선택할 수 있는 대상·정보·보류·선택적 행동은 기존 custom 계약의 입력·후보·결과 형태로 연결한다. 같은 종류의 정보 선택·플레이어 선택·공개·저장 실패는 공통 흐름으로 처리하며, 캐릭터별 규칙 예외를 화면에 중복 구현하지 않는다. Core가 거부한 명령은 상태·이벤트·저장에 부분 반영하지 않는다. 질의와 제안만으로 확정 기록이나 진행 위치를 바꾸지 않는다.

공개 화면은 custom이 제공한 공개 전용 payload만 받는다. 전체 replay 상태, 비공개 계산 근거, 숨은 역할 및 다른 플레이어의 정보를 부수적으로 전달하지 않는다. 다시 보기는 확정 당시 정보를 사용하고 이벤트를 추가하지 않는다. 탭 이동은 현재 확정 진행·입력 맥락을 유지하고, 화면 이동 자체로 행동을 확정하지 않는다.

공식·커스텀 타입·검증기·WASM·저장 책임의 분리를 유지한다. 시나리오 JSON의 기존 파일 형식 버전은 유지하되, 이번 연결을 이유로 시나리오 revision·ruleset version이나 과거 게임 migration을 추가하지 않는다.

외부 공식 custom-script JSON 호환, 이후 밤 UI·실행, 새 캐릭터 능력·활성화 정책, BMR·Homebrew·온라인 공유, 공식 페이지 재설계, 앱 내부 시나리오 보관, #218 이야기형 마도서 테마는 제외한다. 승인된 시각·상호작용의 실질적 변경이 필요하면 기존 승인으로 간주하지 않는다.

### 8. 블랙박스 수용 사례

| ID | 상황·조작 | 관찰 가능한 결과 |
| --- | --- | --- |
| C01 | 시나리오 시작 화면 열기 | 새 작성·JSON 불러오기 두 선택만 표시. 저장본 목록·내부 보관 없음 |
| C02 | 유효한 시나리오 JSON 또는 지원 커스텀 GameFile 불러오기 | 파일 목적 선택·중간 확인 없이 최종 검토로 이동. 재개는 유효한 게임이 있을 때만 판단 |
| C03 | JSON 파일 취소·읽기 실패·검증 실패 | 취소는 현재 상태 유지, 실패는 파일 선택 화면에서 오류. 현재 편집 내용과 기존 게임 보존 |
| C04 | 최종 검토에서 시나리오 저장 후 같은 JSON을 다시 불러옴 | 즉시 다운로드 및 #197 파일 왕복. 내부 시나리오 저장본·게임 생성이나 교체 없음 |
| C05 | 기존 게임 유무와 관계없이 유효한 시나리오에서 새 마도서 쓰기 | 추가 확인 없이 TB/SnV 계열 로딩 전환 후 SnV 형태 게임 설정으로 이동. 별도 행동 목록·미리보기 없음. 진입만으로 기존 게임 불변 |
| C06 | 실제 배역과 좌석을 정하고 유효한 Setup 확정 | 선택한 definition snapshot으로 첫날 밤 진행 |
| C07 | 시나리오 밖 배역·허용되지 않은 Shown Character·bluff·분포로 시작 시도 | 기존 규칙에 따라 거부하고 설정 수정 가능. 잘못된 게임 시작 없음 |
| C08 | 권장 후보 수는 부족하지만 시나리오와 실제 Setup은 유효 | 부족 경고만으로 저장·시작을 차단하지 않음 |
| C09 | 캐릭터 풀 편집 후 순서 검토·새 마도서 생성 | #197의 상대 순서 보존·추가·제거 결과가 유지되고 확정된 순서를 사용 |
| C10 | 복원 가능한 custom GameFile과 원래 definition으로 이어 쓰기 | 같은 게임 상태·진행 위치에서 재개 |
| C11 | 이름·후보 구성·캐릭터 배열 순서·밤 순서 중 하나 변경 | 이어 쓰기 불가, 새 마도서만 가능. 기존 게임 불변 |
| C12 | C11의 변경을 원래 definition과 정확히 같게 복구 | 이어 쓰기 가능 상태 복구 |
| C13 | 동일한 내용의 시나리오 JSON만 불러오기 | 기존 게임을 자동 탐색·연결하지 않으며 이어 쓰기를 새로 활성화하지 않음 |
| C14 | 공식 게임 파일을 커스텀 진입에 제공하거나 기존 공식 화면에서 재개 | 커스텀 변환·자동 공식 우회·분기 대화상자 없음. 기존 공식 화면의 재개 계약 유지 |
| C15 | 첫날 밤 준비·정보·행동·후속 결과 확정 | #203 승인 규칙에 따라 누락·중복 없이 Day 또는 종료 도달 |
| C16 | 진행 중 자동 저장·reload·Undo·게임 파일 왕복 | 같은 기록 구간의 상태·진행 위치·과거 전달 정보 복원 |
| C17 | 잘못된 파일·순서·기록 또는 검증기 실패 | 부분 채택·기본값 보완·공식 실행 우회 없이 오류 처리, 기존 데이터 보존 |
| C18 | 저장 실패 또는 읽을 수 없는 저장본 | 성공 오표시·자동 초기화 없음. 재시도 또는 명시적 복구 동선 제공 |
| C19 | 파일 읽기 중 다른 편집 대상으로 전환하거나 파일 선택 취소 | 늦은 결과나 취소가 현재 내용을 덮어쓰지 않음 |
| C20 | 커스텀 작업 전후 공식 TB·SnV 이동·저장·복원 | 기존 공식 화면·동작·저장 값 유지, 커스텀 데이터 노출 없음 |
| C21 | 모바일·iPad·데스크톱에서 작성 → 게임 설정 → 첫날 밤 조작 | #205 최종 승인 편집 화면과 기존 SnV 형태 마도서의 반응형·스크롤·키보드·터치 동작으로 필요한 입력과 행동에 접근 가능 |

### 8.1. 연결 경계의 추가 수용 사례

| ID | 상황·입력 또는 조작 | 관찰 가능한 기대 결과·보호 계약 |
| --- | --- | --- |
| C22 | 최종 검토에서 유효성을 확인한 뒤 이름·pool·순서를 변경하고 이전 비동기 결과가 도착 | 현재 편집본에 대한 검증만 채택. 예전 definition으로 다운로드·게임 생성하지 않음 |
| C23 | 25명 후보 시나리오로 새 마도서 진입 | actual roster는 비어 있음. 5–15인 설정에 맞는 실제 선택과 좌석 입력을 별도로 요구 |
| C24 | 인원 또는 Setup 변경 역할을 바꾸는 동안 이전 분포 질의가 늦게 도착 | 현재 인원·배역에 대한 custom 분포만 표시·검증. 오래된 성공 결과로 시작 불가 |
| C25 | 유효한 Setup 확정 명령은 수락됐지만 첫 저장 실패 후 재시도 | 설정을 저장하지 못한 상태와 재시도 표시. 동일한 수락 이벤트를 저장하며 Setup 사건 중복 없음. 저장 성공 전 게임 시작 완료로 오표시하지 않음 |
| C26 | 첫날 밤 행동 확정 후 자동 저장 실패 | 확정된 현재 진행과 마지막 저장 상태를 구별해 표시. 재시도는 현재 canonical을 저장하고 행동 재실행·중복 전달 없음 |
| C27 | 하수인·악마 정보 공개, 가리기, 다시 보기, 다음으로 | 공개 중 이야기꾼 화면·게임 조작은 보이지 않고 조작 불가. 가린 뒤 돌아오며 다시 보기는 같은 정보. 다음 행동은 기존 세션 규칙대로 한 번 진행 |
| C28 | 악마 bluff 선택이 0–2개이거나 금지 후보가 포함됨 | 공개 확정 불가. 시나리오·실제 게임 상태에서 허용된 3개로 수정하면 기존 Core 검증 후 공개 가능 |
| C29 | #209 R1/R2의 능력 획득·필수 준비·서로 다른 소유자의 동일 행동을 진행 | 준비와 전달을 별도로 표시·확정하고 각 소유자/출처의 행동이 누락·합쳐지지 않음. 미해결 필수 준비를 둔 새벽 진입 거부 |
| C30 | #209 R4/R5의 재준비·사악한 쌍둥이 재지정과 재통지 | 현재 필요한 준비·공개를 제시하고 이전 전달은 당시 snapshot으로 다시 보기. 재지정 후 옛 후보를 확정할 수 없음 |
| C31 | #209 R11의 선택적 행동으로 게임 종료, 이어서 일반 진행 또는 Undo 시도 | 종료 즉시 밤 진행·Day 진입 차단. 허용된 Undo는 기존 단위대로 직전 사실·대기 행동을 복원 |
| C32 | 제안 또는 대상 선택 후 다른 확정·Undo로 진행 상태가 바뀐 상태에서 옛 선택 확정 | 오래된 occurrence/제안 거부, 현재 상태 유지. 최신 입력으로 다시 선택할 수 있음 |
| C33 | 정상 이벤트 prefix 뒤 잘못된 사건을 붙인 GameFile 불러오기 | 전체 복원 거부. 정상 prefix만 게임으로 채택하거나 저장하지 않음 |
| C34 | 정상 게임을 JSON으로 내보내고 새로 불러와 이어 쓰기 | definition ID·순서·확정 이벤트·지원 UI 좌석 정보 보존. 임의의 새 definition ID를 부여하지 않음 |
| C35 | 시나리오 전용 JSON을 여러 번 불러오기 | 매번 독립 작성 identity로 시작. 내용이 같아도 브라우저 게임을 자동 탐색해 이어 붙이지 않음 |
| C36 | Core/WASM 초기화 실패 또는 파일 판독 중 다른 작업으로 이동 | 로딩 성공 화면으로 넘어가지 않으며 현재 편집/게임 보존과 재시도 제공. 다른 runtime·fixture로 우회하지 않음 |
| C37 | 게임 진행·정보 공개 화면에서 새로고침 후 세션 복원 | 마지막 저장 canonical에서 같은 행동/확정 당시 전달 복원. 로드 직후 비공개 정보를 플레이어 공개 화면으로 자동 노출하지 않음 |
| C38 | 첫날 밤 종료 또는 Day GameFile을 읽음 | 기존 Core가 지원하는 상태를 복원하되 이 이슈 범위 밖인 낮 능력·다음 밤을 활성화하거나 실행 완료로 가장하지 않음 |

C29–C31의 세부 입력과 독립 기대값은 `docs/acceptance/issue-209-mixed-first-night.md`가 참조하는 승인된 #209 Spec 사례와 R0–R11을 그대로 계승한다. 이번 연결은 그 결과를 실제 화면에서 선택·확정·전달·복원할 수 있어야 하며, 웹 화면에서 별도 정답을 계산해 검증 기준으로 삼지 않는다.

### 9. 검증과 완료 판단

- 기존 #197 작성·JSON 왕복 검증을 유지하며, 이번에 연결하는 마도서·실제 WASM·세션·저장 경계의 수용 근거를 추가한다.
- 실제 Production 흐름에서 새 작성 또는 JSON 불러오기 → 최종 검토 → 게임 설정 → 첫날 밤 진행과 게임 재개·복원을 확인한다. fixture만 연결된 화면이나 버튼 활성화만으로 완료하지 않는다.
- 기존 공식 TB·SnV의 화면·파일·저장 회귀와 커스텀 격리를 확인한다.
- 프로젝트 필수 Rust/web 검사, 관련 `pnpm --dir web build`, Production 브라우저 수용 검증을 통과한다. 실행 환경과 실기기 검증 여부를 구분해 기록한다.
- 세부 테스트 코드·파일 변경·작업 순서는 Plan 및 test 단계에서 다룬다.
- C01–C38 및 확정될 S1 수용 사례와 실제 연결 범위의 검증을 완료하기 전에는 #220 전체 완료로 판정하지 않는다. #205/#218의 완료나 이야기형 UI 승인을 대신하지 않는다.

### 10. 원본 미결정 항목 정리와 분기 범위

| 원본 ID | #220에서 적용할 결정 |
| --- | --- |
| D1 내부 시나리오 보관 시점 | 최종 승인으로 삭제. JSON 다운로드·불러오기만 사용 |
| D2 내부 시나리오 교체·복구 | 내부 보관 기능과 함께 삭제. 게임 진행 저장·복구와 혼동하지 않음 |
| D3 내부 보관과 새 마도서 관계 | 내부 보관 선행 조건 없음. 새 마도서는 확인 없이 게임 설정으로 이동하며 진입 자체가 기존 게임을 폐기하지 않음 |
| D4 파일 진입 동선 | 하나의 JSON 진입, 성공 즉시 최종 검토. 목적 선택·중간 확인·공식 마도서 분기 UI 없음 |
| D5 진행 저장·복구 실패 | 기존 세션 계약에 따라 저장 실패 표시·현재/마지막 저장 상태 구분·재시도·기존 데이터 보존. 기존 슬롯 충돌·명시적 교체 정책은 12절 S1에서 사용자 결정 대기 |

D1–D4를 미결정으로 다시 열거나 제거된 기능에 대한 승인을 다시 요청하지 않는다. D5의 저장 실패·재시도는 기존 세션 계약을 재사용한다. 코드 대조에서 발견된 정상/손상 슬롯의 명시적 교체 시점과 범위는 12절 S1에서 사용자 결정을 받은 뒤 확정한다.

#220의 마도서 표현은 기존 SnV 형태다. 시나리오 편집 화면의 기준은 develop에 남은 이전 #197 UI가 아니라 #205 최종 승인본이다. #218의 문장·페이지·이야기형 정보 전달 디자인은 이 Spec의 구현 선행 조건도, #220에 추가할 UI도 아니다.

현재 Setup 연결 코드는 부분 구현으로 보존하고 이 Spec에 맞춰 이어 간다. 승인된 편집 화면 이식, 실제 JSON 동선, 첫날 밤 전체 실행, 지속 저장·재개·복원은 각각 실제 구현·검증 근거가 갖춰져야 완료로 기록한다.

### 11. 코드 대조 결과와 실제 변경 범위

| 현재 근거 | 유지할 계약 | #220에서 남은 연결 |
| --- | --- | --- |
| `customScenarioLanding`과 #197 authoring controller | 현재 Production 진입, 편집·검증·파일 비동기 최신성 | #205 승인 UI 반영, 실제 게임 파일의 공통 JSON 진입, 마도서 진입·재개 연결 |
| `scenarioFile`의 type `clocktower-custom-scenario`, version 1 | name·characterIds·firstNightOrder만 내보내며 definition ID·게임 기록 없음 | 새로 불러온 시나리오에 독립 ID 부여하는 기존 정책 유지 |
| custom `GameFile` schemaVersion 4 | 완전한 definition snapshot·game ID·확정 이벤트 및 지원 UI 좌석 정보 | parse 후 전체 replay 성공을 거쳐 review/resume. 기존 JSON 형식 변경 없음 |
| `sameCustomScriptDefinition` | ID·이름·배열과 action의 정확한 순서 비교 | 화면에서 수정 시 이어 쓰기 숨김·정확 복원 시 표시 |
| `GrimoireSetupController` 부분 구현 | custom 분포·createGame·첫 저장 확인·수락 이벤트 재저장 | 승인 UI와 연결; 현재 모든 기존 저장을 거부하는 임시 경계는 S1 결정 후 일치시킴 |
| `CustomCanonicalSession` 및 canonical controller | 명령/replay 성공 후 메모리 채택; 저장 실패 별도; Undo·과거 정보 복원 | 첫날 밤 전체 행동·정보·필수 준비·선택적 행동의 사용자 조작과 오류 표시 |
| IndexedDB session driver | `customScriptId` 기준 저장 슬롯 하나, 정상 쓰기와 손상 복구 구분 | S1에 따른 명시적 교체 확인과 실패 보존 |
| 공용 표현 shell/catalog/board/reveal | 기존 SnV 화면 구조·스타일과 입력 접근성 | 공식 컨트롤러 대신 custom 데이터·콜백을 공급하는 연결 계층 |

기존 정상 슬롯의 `saveSession`은 같은 시나리오의 다른 게임도 덮어쓸 수 있으므로, 저장 드라이버가 사용자 교체 의사를 확인해 준다고 가정하지 않는다. 부분 Setup 구현은 반대로 기존 슬롯을 모두 거부한다. 두 코드의 현재 동작 어느 쪽도 최종 제품 정책으로 자동 채택하지 않는다.

Setup 전 시나리오 편집본은 영구 보관하지 않는다. 확정 게임의 definition snapshot을 세션이 저장하는 것은 삭제하기로 한 ‘내부 시나리오 저장 목록’과 다른 책임이다. 기존 definition repository나 과거 저장본을 자동 삭제·migration하지 않는다.

### 12. S1 — 자동 저장 슬롯 충돌·복구 정책 (사용자 결정 대기)

현재 질문: 동일 시나리오의 새 게임을 시작할 때 기존 단일 슬롯을 명시적으로 교체할지, 여러 게임을 보관하는 구조로 확장할지.

**권장안 초안: 시나리오당 슬롯 하나 유지, 실제 교체 직전에 확인.**

- `새 마도서 쓰기`는 확인 없이 설정으로 이동한다. 슬롯이 비어 있으면 기존 Setup 확정 흐름으로 시작한다.
- 기존 자동 저장이 있으면 `게임 설정 확정` 때 교체 대상과 기존 자동 저장이 바뀐다는 점을 보여주고 확인/취소한다. 취소하면 설정 입력과 기존 게임을 유지한다. 확인 후에도 Setup 검증 및 새 저장이 성공하기 전에는 기존 게임을 삭제하지 않는다.
- 손상된 슬롯은 정상 빈 슬롯처럼 다루지 않는다. 손상 사실과 교체 범위를 알리고 명시적 복구 확인을 거친 뒤에만 새 게임으로 대체한다. 단순 읽기/권한 오류는 손상으로 단정하지 않고 재시도한다.
- 불러온 GameFile은 검증·최종 검토만으로 자동 저장을 교체하지 않는다. 이어 쓰기가 실제로 기존 슬롯의 다른 게임 또는 다른 기록을 대체하는 경우에도 교체 직전에 같은 확인·실패 보존 정책을 적용하는 것을 제안한다. 완전히 같은 저장 기록의 재개는 교체 확인 대상이 아니다.
- 확인을 기다리는 동안 대상 저장이 바뀌면 이전 확인으로 다른 기록을 덮어쓰지 않는다. 바뀐 대상에 대해 다시 판단할 수 있게 한다.

| ID | 상황·조작 | 권장안 채택 시 기대 결과 |
| --- | --- | --- |
| S1-a | 기존 게임이 있는 시나리오에서 새 마도서 쓰기 → 설정 입력 | 확인 없이 설정 진입, 기존 저장 불변 |
| S1-b | 위 설정 확정에서 교체 취소 | 입력한 설정과 이전 게임 유지, 첫날 밤 시작 안 됨 |
| S1-c | 교체 확인 후 새 Setup 검증 또는 저장 실패 | 기존 저장 보존, 원인 표시·재시도. 이전 게임 삭제나 새 게임 성공 오표시 없음 |
| S1-d | 교체 확인 후 Setup와 저장 성공 | 새 게임이 해당 슬롯의 재개 대상이 됨. 다른 시나리오·공식 저장은 불변 |
| S1-e | 손상 슬롯으로 새 게임 또는 파일 재개 | 손상 자동 덮어쓰기 없음. 명시적 교체 선택에서만 성공 시 대체, 취소/실패는 원본 보존 |
| S1-f | GameFile을 불러와 최종 검토만 함, 또는 다른 기록으로 이어 쓰기 | 검토만으로 저장 불변. 실제 충돌 재개는 확인/취소 및 성공 전 보존 정책 적용 |
| S1-g | 대상 저장이 확인 도중 바뀜 | 이전 확인으로 바뀐 기록을 덮어쓰지 않음 |

여러 게임 보관안을 선택하면 game ID별 저장과 사용자 재개 대상 선택, 기존 슬롯 처리 정책이 추가로 필요하다. 현재 원본 계약을 변경하므로 이 초안으로 그 확장을 확정하지 않는다.

이 절의 선택과 전체 문안 승인이 끝나기 전에는 Spec 완료나 Plan 착수를 선언하지 않는다. 코드·테스트·구현 파일 배치와 작업 순서는 이 문서의 범위가 아니다.

# 2026-09-12 추가 인수 피드백 검증

## 원인과 수정

- 진행 목록: 실행 묶음의 생성 순서를 사용하여 준비 행동이 있는 세탁부·사서가 앞에 나왔다. Core `phaseOverview`의 시나리오 순서로 묶음을 정렬한다. 실행/Undo 경계와 Core 스케줄러는 변경하지 않았다.
- 요리사 숫자 입력: 공유 TB 컴포넌트에 해당하는 원본 CSS가 custom에 포함되지 않았다. TB의 입력 폭/높이/폰트, 숫자와 단위의 flex 배치를 공유 스타일에 연결하고 BMR 색상을 적용했다.
- 중독 공개 강조: 이전 정보 영향 연결 수정이 적용된 최신 빌드에서는 유효한 값 입력 후 보라색 강조가 표시된다. 추가 제품 변경 없이 실제 computed style과 정보 공개까지 회귀 검증했다. 미입력/유효하지 않은 값의 비활성 버튼은 기존 흐림 표시를 유지한다.

## 데이터와 검증

`test0912-game-2.json`은 사용자 첨부 원본이다. `test0912-game-2-chef.json`은 동일 게임에서 Core 명령으로 하수인·악마·독살범·세탁부·사서를 진행한 파생 저장 파일이다. 회귀 테스트는 원본에서 요리사까지 다시 실행한다.

- 수정 전 목록 회귀: 실패(세탁부·사서가 하수인보다 앞섬). 숫자 입력의 원본 스타일 누락은 브라우저 화면으로 확인.
- 통합 및 TypeScript 검사: 787 통과.
- 브라우저: 요리사 320/390/820/1366px, 원본 시작 목록 390/1366px, 이전 보르톡스 4개 너비 — 총 10 통과.
- 실제 숫자 입력, 공개 버튼 활성화/보라색, 입력 크기/단위 배치, 정보 공개, 목록과 현재 행동 일치 확인.
- [320px 요리사](issue-220-order-poison-evidence/chef-320.png), [1366px 요리사](issue-220-order-poison-evidence/chef-1366.png), [390px 시작 목록](issue-220-order-poison-evidence/order-390.png)을 직접 확인.
- production build/PWA/architecture/diff check 통과. 리뷰 서버의 최신 JS/CSS와 dist 바이트 일치, keep 유지.

## 사용자 확인

원본 첨부를 이어 쓴 뒤 하수인부터 표시되는 목록을 확인한다. 중독 요리사에서 전달 숫자를 입력하고 보라색 `중독 정보 공개` 버튼 및 숫자 옆 `쌍` 표시를 확인한다.
